apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-storage
  namespace: todo-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard-rwo    # GKE's default SSD storage class
  resources:
    requests:
      storage: 10Gi
